unit UEditArt;interface	uses		UDialog, UList, UWolfDoc, UArtView;	type		TArtEditDialog = object(TDialog)				fTitlePrefix: StringHandle;				fMapList: TMapListDoc;				fArtView: TArtView;				fThumbNailView: TPixMapView;				fCurrentItem: integer;				fPalette: TImagePalette;				fCurrentArt: integer;				procedure IArtEditDialog (itsID: integer; itsPalette: TImagePalette; itsMapList: TMapListDoc; thumbs: boolean);				procedure Free;				override;				procedure FrameItem (itemNo: integer; pat: Pattern);				procedure SetCurrentView (itemNo: integer);				procedure DrawUserItem (itemNo: integer);				override;				procedure ItemHit (var e: EventInfo; itemNo: integer);				override;				procedure Content (var e: EventInfo);				override;				procedure SetupMenus;				override;				procedure DoMenuCommand (cmdNumber: integer);				override;				procedure CutArt;				procedure CopyArt;				procedure PasteArt;				procedure ClearArt;				procedure CutThumbNail;				procedure CopyThumbNail;				procedure PasteThumbNail;				procedure ClearThumbNail;				procedure SelectArt (artNo: integer);				procedure InstallArt;				procedure UninstallArt;				function ArtImageType: CustomImageType;				function ArtImageCode: integer;				function HasUserImage: boolean;				procedure HaveUserImage (val: Boolean);				procedure InstallFlag (value: boolean; itemNo: integer);				procedure UninstallFlag (var flag: boolean; itemNo: integer);				procedure UpdateImage (gworld: GWorldPtr; isThumbnail: boolean);				procedure ChangeImage (gworld: GWorldPtr; isThumbnail: boolean);				procedure ChangeDoorImage (code: integer; gworld: GWorldPtr; shrink: boolean);				procedure ChangeObjectImage (code: integer; gworld: GWorldPtr);				procedure InstallThumbNail;				procedure Deactivate;				override;				procedure UpdateTitle;				override;			end;		TImagePalette = object(TList)				fNumItems: integer;				fDialog: TArtEditDialog;				procedure IImagePalette (itsCols, itsNumItems: integer);				procedure DrawCell (cell: Point; r: Rect; var hilite: boolean);				override;				procedure ChangeCellHilite (cell: Point; hilite: boolean);				override;				procedure TImagePalette.SetSelectionRect (newSel: Rect);				override;				procedure DrawImage (itemNo: integer; r: Rect);				function ItemNumber (cell: Point): integer;				procedure ItemSelected (itemNum: integer);				procedure InvalidateItem (itemNo: integer);				function ItemSelectable (item: integer): boolean;			end;implementationend.